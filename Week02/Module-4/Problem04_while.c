#include <stdio.h>

int main()
{
    int n;
    printf("Input number of terms: ");
    scanf("%d", &n);

    while (n--)
    
    
    {
        /* code */
        printf("Number is: %d and Square of %d is : %d\n", n, n,n*n);
    }
    

    return 0;
}